services:
  chatapp.gateway:
    image: ${DOCKER_REGISTRY-}chatappgateway
    build:
      context: .
      dockerfile: src/Gateway/ChatApp.Gateway/Dockerfile

  chatservice.api:
    image: ${DOCKER_REGISTRY-}chatserviceapi
    build:
      context: .
      dockerfile: src/ChatService/ChatService.Api/Dockerfile

